# SERVER-SIDE API BUILD SOME MORE IMPORTANT PACKAGES.
1. npm i express.
2. npm i mongoose.
3. 1. npm i eslint --save-dev (for development dependecies).
3. 2. npx i eslint --init
3. 3. both syntax run for only one time use (npm init @eslint/config@latest -- --config eslint-config-standard).
3. 4. npm init @eslint/config@latest (only write this command)
